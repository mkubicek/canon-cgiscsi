[project]
name = "canon-cgiscsi"
version = "0.1.0"
description = "Clean-room Python client for Canon's cgiscsi document scanner protocol"
requires-python = ">=3.11"
dependencies = [
    "img2pdf>=0.5.1",
    "ocrmypdf>=17.0.0",
    "pillow>=10.0.0",
]

[tool.uv]
package = false
